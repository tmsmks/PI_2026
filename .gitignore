.venv/
.claude/
__pycache__/
*.pyc
*.pyo
.DS_Store
.env
*.egg-info/
dist/
build/
.ipynb_checkpoints/

# Datasets volumineux régénérés par le pipeline (gardés en local uniquement).
# Reproductibles via `python run_pipeline.py` ; ils polluaient `git status`
# (CSV réécrits à chaque run). Pour cesser de suivre ceux déjà versionnés :
#   git rm -r --cached data/raw data/processed data/features
data/raw/
data/processed/
data/features/
